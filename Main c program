#include<stdio.h>

int main(){
    int n;
    while(1){
        int stdntPanel;
        int admPanel;
        printf("\n\t\t\t   1.stdntPanel \n\t\t\t   2.admPanel \n\t\t\t   3.Exit \n\t\t\t   Option:");
		int in;
		printf("enter the input");
        scanf(" %d",&in);
        if(in==1)
        {
                int c=0;
                printf("vote goes to student panel:%d",stdntPanel=1);   
                 c=c+1;
                printf("the vote count:%d",c) ;
        }
        else if(in==2)
        {
        	int cc=0;
        	printf("vote goes to admin panel:%d",admPanel=1);
        cc=cc+1;	
    }
    else{
    	printf("no vote");
    
}
    }
}
